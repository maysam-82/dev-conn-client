import React, { Fragment } from 'react';
import { Navbar } from '../Navbar';
import { Landing } from '../Landing';

const App = () => {
    return (
        <Fragment>
            <Navbar />
            <Landing />
        </Fragment>
    );
};

export default App;
